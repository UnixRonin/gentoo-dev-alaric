--- get_file.c	2016-02-10 12:02:45.494226182 -0500
+++ get_file.c	2016-02-10 12:05:34.731318670 -0500
@@ -1058,18 +1058,31 @@
 */
 void SetWindowName ( text )
     Text *text;
 {
     char *icon_name;
+    char hostname[MAXHOSTNAMELEN + 1];
+    char windowtitle [MAXPATHLEN + MAXHOSTNAMELEN + 2];
     
-    icon_name = strrchr ( text -> filename, '/' );
+    gethostname(hostname, sizeof hostname);
+    hostname[MAXHOSTNAMELEN] = 0; /* make good and certain the hostname is null-terminated */
+
+    snprintf(windowtitle,
+            MAXPATHLEN + MAXHOSTNAMELEN + 1,
+            "%s:%s",
+            hostname,
+            text -> filename);
+    windowtitle[MAXPATHLEN + MAXHOSTNAMELEN + 1] = 0;  /* same as above */
+
+    icon_name = strrchr ( windowtitle, '/' );
     if ( icon_name )
       icon_name ++;
     else
-      icon_name = text -> filename;
+      icon_name = windowtitle;
+
     XSetIconName ( dpy,  text ->  w_parent, icon_name );
-    XStoreName ( dpy, text ->  w_parent, text -> filename );
+    XStoreName ( dpy, text ->  w_parent, windowtitle );
 }
 
 /*
 **	Function name : update_cwd
 **
diff -U5 -Bb xcoral-3.47/macros.c xcoral-3.47-r1/macros.c
--- macros.c	1998-09-20 12:16:05.000000000 -0400
+++ macros.c	2015-02-18 10:51:30.010123656 -0500
@@ -48,10 +48,12 @@
 #  if HAVE_MALLOC_H
 #    include <malloc.h>
 #  endif
 #endif
 
+#include <strings.h> /* for bzero prototype */
+
 #include "main_text.h"
 #include "macros.h"
 #include "warn_box.h"
 #include "main_events.h"
 #include "get_file.h"
